{% extends 'base.html.twig' %}

{%
    set parameters = {
    'app_name': APP_NAME,
    'app_urldomain': APP_URLDOMAIN,
}
%}

{% block title %}{{ parent() }} - {{ 'menu.privacy' | trans }}{% endblock %}

{% block body %}
    {{ 'info.privacy' | trans(parameters, APP_CUSTOM_TRANSLATION ) | raw }}

    <p><a href="{{ path('app_info_index') }}">{{ 'button.back_to' | trans({'%value%': APP_URLDOMAIN}) }}</a></p>
{% endblock %}
